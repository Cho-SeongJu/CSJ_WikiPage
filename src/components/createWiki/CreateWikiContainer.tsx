import CreateWikiForm from "./CreateWikiForm";

const CreateWikiContainer = () => {
  return (
    <div className="w-screen h-screen flex flex-col items-center justify-center">
      <CreateWikiForm />
    </div>
  );
};

export default CreateWikiContainer;
